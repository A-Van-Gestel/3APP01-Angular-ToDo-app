import { ValidColorHexValidator } from './valid-color-hex-validator';

describe('ValidColorHexValidator', () => {
  it('should create an instance', () => {
    expect(new ValidColorHexValidator()).toBeTruthy();
  });
});
